## 归类汇总

- 2.3 任务推进与收敛失衡 -> 边查边改，无阶段性总结
  Session: `69df9519dbc2615630b6f71b`
- 2.3 任务推进与收敛失衡 -> 未完成任务就戛然而止
  Session: `69df9519dbc2615630b6f71b`

## 明细表

| 任务题ID | Conversation ID | Session ID | Bad Pattern | Bad Pattern 描述 | 如何做会更好 | 可能缺少的知识点 |
| --- | --- | --- | --- | --- | --- | --- |
| 未提供 | 未提供 | 69df9519dbc2615630b6f71b | 边查边改，无阶段性总结 | 当前项目对应的 Trae `workspaceStorage` 会话是 `69df9519dbc2615630b6f71b`，模型映射为 `0414-dyad`。在这轮执行中，Agent 没有先把任务收敛到一个较小可交付子集，而是连续在多个文件上展开自动生成和补写：日志可见其依次新增/修改 `AdminMapper.java`、`ScenicSpotMapper.java`、`ReviewMapper.java`、`FavoriteMapper.java`、`LoginDTO.java`、`RegisterDTO.java`、`UserService.java` 等文件（`ai-agent_0_1776173469869_stdout.log` 行 551442、551803、551972、552304、552450、552635、552825）。同一 session 后续快照统计里又显示 `diff_file_infos len: 65`、`file_count=65`（同日志行 660870-660871），说明它在没有形成阶段性收敛和验证闭环的情况下持续扩散修改范围。这个轨迹符合“连续修改 >3 个不同文件、缺乏 checkpoint”的特征。 | 先把宏观需求切成可验收的子目标，例如“先完成用户登录注册主链路并通过编译/启动验证”，每完成一组相关文件后立刻做一次编译、运行或阶段总结，再决定是否继续扩展到下一个模块。 | 大任务分解、阶段性 checkpoint 设计、面向交付的范围控制、长链路代码生成中的收敛策略。 |
| 未提供 | 未提供 | 69df9519dbc2615630b6f71b | 未完成任务就戛然而止 | 这个 session 不是“用户主动中断”，而是明确撞到了思考/turn 上限。日志里当前 session 三次都出现了同一类配置和报错：`0414-dyad` 在该 session 的 `model_config` 记录中带有 `max_turn: 50, prompt_max_tokens: 45000, max_tokens: 16000`（同日志行 539293、557870、599012）；随后分别报出 `the task is not completed due to max step reached`、`Error 4000002: exceeded maximum number of turns`（同日志行 556144-556153、572877-572894、634083-634092）。与此同时，日志还记录了用户在这个 session 中多次输入 `继续`（同日志行 557856、598977），`export_past_chat` 则表明这个 session 最终只有 3 个响应消息回合（同日志行 665178-665183）。这说明 Agent 每次都在任务尚未完成时耗尽 turn budget，被迫停下，用户只能再输入一次“继续”续跑。 | 在接近预算边界前主动输出当前完成度、剩余范围和下一步建议；优先交付一个最小可用子系统，而不是在单轮中试图铺满整个旅游系统骨架。若必须继续，应先总结当前成果和未完成项，再以新的明确子任务进入下一轮。 | 对平台 step/turn 预算的感知、预算内交付策略、超长任务的阶段封包与续跑设计。 |

## 思考上限原因分析

1. 这次不是 Trae 前端随机弹了一个“继续”，而是后端明确返回了超限错误。当前 session `69df9519dbc2615630b6f71b` 在日志中三次出现 `4000002 / exceeded maximum number of turns`，对应时间分别是 2026-04-15 21:45:38、21:54:50、22:55:45。
2. 触发上限的直接原因，是 Agent 在一个很宽的大需求下持续扩文件、扩模块，单轮里消耗了大量 tool steps，却没有形成阶段性封口。它不是被某一个编译错误卡死，而是“工作面越铺越大，最后把 turn budget 用完”。
3. 当前 session 还伴随出现了多次 `Failed to send to stream: SendError { .. }` 日志，但从时序看，这些错误后 Agent 仍继续写文件、更新 snapshot，并最终由 `max step reached` 结束，因此它们更像伴随性的流式通道噪音，不是这次反复要求“继续”的主因。

## 结论

是，当前这个 Agent 执行存在问题，但主问题更像“执行策略与收敛能力差”，不只是简单的模型思考次数少或者平台偶发故障。它在广义目标下不断扩张改动范围、缺少阶段性校验和交付封口，最终反复撞上 `max_turn` 上限，才导致用户需要持续输入“继续”。
